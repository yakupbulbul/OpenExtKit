# Using with Windsurf

Windsurf can connect to OpenExtKit through the same local MCP command:

```sh
openext mcp
```

Use workspace-root projects. The server only reads and writes within the configured workspace and keeps browser tests isolated from real user profiles.
