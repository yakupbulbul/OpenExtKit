# Using with Claude Code

Configure Claude Code to start the local server with:

```sh
openext mcp
```

Keep the MCP working directory at the root of the extension project. The server rejects paths outside that workspace and does not expose arbitrary shell execution.

Use the release report tool before asking an AI assistant to prepare publishing notes.
