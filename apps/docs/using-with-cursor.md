# Using with Cursor

Cursor can use the OpenExtKit MCP server to inspect and operate an extension project through structured tools.

Set the MCP command to:

```sh
openext mcp
```

Prefer MCP tools for OpenExtKit actions so builds, audits, tests, and packages are logged in `.openextkit/audit.log`.
