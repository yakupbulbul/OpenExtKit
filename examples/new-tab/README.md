# New Tab Example

A new-tab style extension surface built as an HTML entrypoint.

The current manifest generator exposes this page through the options UI field until browser override support lands in the target extensibility phase.

## Commands

```sh
pnpm build
pnpm package
pnpm test
```
