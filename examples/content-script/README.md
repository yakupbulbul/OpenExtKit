# Content Script Example

A content script extension that annotates pages with a small OpenExtKit badge.

The example uses `<all_urls>` so permission audits demonstrate broad host permission warnings.

## Commands

```sh
pnpm build
pnpm package
pnpm test
```
