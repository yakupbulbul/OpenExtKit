# React Popup Example

A popup-oriented extension example using a React-style `.tsx` popup entrypoint.

The current OpenExtKit packager copies configured source files into target output. A framework bundler adapter can later compile this entrypoint before packaging.

## Commands

```sh
pnpm build
pnpm package
pnpm test
```
