{
  "name": "@openextkit/cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "openext": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "node --test"
  },
  "dependencies": {},
  "devDependencies": {}
}
