# Architecture

OpenExtKit uses an adapter-based architecture. Core configuration is browser-neutral, and browser-specific packages convert that configuration into manifests, reports, build output, and test runs.

The system is designed around small packages so developers can depend on only the pieces they need.
