# MCP Tools

OpenExtKit will include an MCP server so AI coding tools can inspect configuration, generate manifests, audit permissions, run checks, and package extensions through explicit local tools.
